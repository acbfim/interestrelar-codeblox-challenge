const protocol = 'https';
const server = 'interestrellar.code.br';
const host = `${protocol}://${server}`;

export const environment = {
  production: true,
  ambiente: 'Production',
  ambienteAbrev: 'PROD',
  urlApi: `${host}/api`,
  secretKey: 'as8&6a*h$#oa(23)K8t$#',
};
